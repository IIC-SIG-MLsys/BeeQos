# ebpf方案好处
在server端启动一个sockperf server容器模拟业务端，不搞优先级只限速，查看限速带来的效果
在client端启动一个sockperf client容器，每个内部运行下面的多线程脚本进行带宽和时延的测量

直接跑不区分优先级的多流限速，测
oncn，tc-htb，cilium，不限速 四种情况的。

测试各自2,4,8,16,32,64,128条流情况下

流间公平性：jain：公平性随并发数变化的趋势（可能优势不大）
p99延迟 和 延迟抖动（Std-Dev）fig1-2：p99延迟 随 并发数上涨的变化，fig1-3：三优先级的抖动 随并发数 上涨变化（相较于实验一冗余？）看edt方案非队列带来的好处

利用率（可能没区别，此图可以不放）


## 测试脚本
复用试验1的脚本